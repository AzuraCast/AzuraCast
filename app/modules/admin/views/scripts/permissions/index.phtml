<?
$title = 'Manage Permissions';
$manual = true;
?>
<div class="block-header">
    <h2>Manage Permissions</h2>
</div>

<div class="row">
    <div class="col-sm-6">
        <div class="card">
            <div class="card-header ch-alt">
                <h2>Roles</h2>
                <a class="btn bgm-blue btn-float" href="<?=$this->url->routeFromHere(array('action' => 'editrole')) ?>"><i class="zmdi zmdi-plus"></i></a>
            </div>
            <div class="table-responsive">
                <table class="table table-striped">
                    <colgroup>
                        <col width="40%" />
                        <col width="60%" />
                    </colgroup>
                    <thead>
                        <tr>
                            <th>&nbsp;</th>
                            <th>Role Name</th>
                        </tr>
                    </thead>
                    <tbody>
                    <? foreach($roles as $role): ?>
                        <tr class="input">
                            <td class="center">
                                <a class="btn btn-sm btn-primary" href="<?=$this->url->routeFromHere(array('action' => 'editrole', 'id' => $role['id'])) ?>">Edit</a>
                            <? if( $role['id'] != 1 ): ?>
                                <a class="btn btn-sm btn-danger" href="<?=$this->url->routeFromHere(array('action' => 'deleterole', 'id' => $role['id'])) ?>">Delete</a>
                            <? else: ?>
                                <a class="btn btn-sm btn-danger disabled" href="#" onclick="alert('This role cannot be deleted.'); return false;">Delete</a>
                            <? endif; ?>
                            </td>
                            <td align="left"><?=$role['name'] ?></td>
                        </tr>
                    <? endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <div class="col-sm-6">
        <div class="card">
            <div class="card-header ch-alt">
                <h2>Actions</h2>
                <a class="btn bgm-blue btn-float" href="<?=$this->url->routeFromHere(array('action' => 'editaction')) ?>"><i class="zmdi zmdi-plus"></i></a>
            </div>
            <div class="table-responsive">
                <table class="table table-striped">
                    <colgroup>
                        <col width="40%" />
                        <col width="60%" />
                    </colgroup>
                    <thead>
                    <tr>
                        <th>&nbsp;</th>
                        <th>Action Name</th>
                    </tr>
                    </thead>
                    <tbody>
                    <? foreach($actions as $action): ?>
                        <tr class="input">
                            <td class="center">
                                <a class="btn btn-sm btn-primary" href="<?=$this->url->routeFromHere(array('action' => 'editaction', 'id' => $action['id'])) ?>">Edit</a>
                                <a class="btn btn-sm btn-danger" href="<?=$this->url->routeFromHere(array('action' => 'deleteaction', 'id' => $action['id'])) ?>">Delete</a>
                            </td>
                            <td align="left"><?=$action['name'] ?></td>
                        </tr>
                    <? endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>