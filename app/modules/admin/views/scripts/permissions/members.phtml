<?
$this->view->setVar('title', 'Members of Role: '.$this->role->name);
?>

<table class="table table-striped">
    <colgroup>
        <col width="15%" />
        <col width="15%" />
        <col width="60%" />
    </colgroup>
    <thead>
        <tr>
            <th>Actions</th>
            <th>UIN</th>
            <th>User Name</th>
        </tr> 
    </thead>
    <tbody>
    <? foreach($this->view->role_members as $member): ?>
        <tr>
            <td align="center">
                <?=$this->viewHelper->button(array(
                    'type'      => 'small',
                    'icon'      => 'delete',
                    'class'     => 'negative confirm-action',
                    'href'      => $this->viewHelper->routeFromHere(array('action' => 'members', 'user_id' => $member['user_id'])),
                    'text'      => 'Delete',
                )) ?>
            </td>
            <td align="center"><?=$member['user']['uin'] ?></td>
            <td align="left"><?=$member['user']['lastname'] ?>, <?=$member['user']['firstname'] ?></td>
        </tr>
    <? endforeach; ?>
    </tbody>
</table>