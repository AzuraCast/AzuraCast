<? foreach($categories as $cat_key => $cat): ?>
    <? if (count($cat['stations']) > 0): ?>
        <div class="floating">
            <div class="well">
                <h3><i class="<?=$cat['icon'] ?>"></i> <?=$cat['name'] ?> <small><span id="nowplaying-listeners-<?=$cat_key ?>"></span></small></h3>

                <?
                $stations = $cat['stations'];
                $render_mode = 'standalone';

                echo $this->viewHelper->renderCommon('station', array('stations' => $stations));
                ?>
            </div>
        </div>
    <? endif; ?>
<? endforeach; ?>