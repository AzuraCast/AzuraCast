<?php
$title = ('Artist Licensing Documentation');
?>

<h4><a href="<?=$this->viewHelper->routeFromHere(array('controller' => 'index', 'action' => 'index')) ?>"><i class="icon-arrow-left"></i> Station Center Home</a></h4>

<h2>Fully Licensed Artists</h2>

<ul class="media-list">
<? foreach($this->artists as $artist): ?>
    <li class="media">
        <img class="media-object pull-left" src="<?=\DF\Url::content($artist['image_url']) ?>" style="width: 40px;">
        <div class="media-body">
            <h4>
                <?=$artist['name'] ?><br>
                <small>
                    <div>
                    <? foreach((array)$artist['types'] as $type): ?>
                        <i class="<?=$type['icon'] ?>"></i> <?=$type['name'] ?> 
                    <? endforeach; ?>
                    </div>
                </small>
            </h4>
            <div><?=ucfirst($artist['license']) ?> License</div>
            <div>Initialed: <?=$artist['initials'] ?></div>
        <? if ($artist['initial_timestamp']): ?>
            <div>Signed and Authenticated <?=$artist['initial_timestamp']->format('m/d/Y g:ia')  ?></div>
        <? endif; ?>
        </div>
    </li>
<? endforeach; ?>
</ul>
