<?
$this->view->setVar('title', 'Station Center');
?>

<h2>Select a Station to View</h2>
<dl>
<? foreach($this->view->stations as $station_id => $station): ?>
    <dt><a href="<?=$this->viewHelper->routeFromHere(array('action' => 'index', 'station' => $station_id)) ?>"><i class="<?=$station->getCategoryIcon() ?>"></i> <?=$station->name ?></a></dt>
<? endforeach; ?>
</dl>