<?
$title = 'Manage Playlists';
$manual = true;
?>

<div class="block-header">
    <h2><?=$station->name ?></h2>
</div>

<div class="card">
    <div class="card-header ch-alt">
        <h2>Station Playlists</h2>

        <a class="btn bgm-blue btn-float" href="<?=$this->url->routeFromHere(array('action' => 'edit')) ?>"><i class="zmdi zmdi-plus"></i></a>
    </div>
    <div class="table-responsive">
        <table class="table table-striped">
            <colgroup>
                <col width="25%">
                <col width="30%">
                <col width="15%">
                <col width="15%">
                <col width="15%">
            </colgroup>
            <thead>
            <tr>
                <th>Actions</th>
                <th>Playlist</th>
                <th>Weight</th>
                <th><abbr title="The likelihood that this playlist will be the source of the next song.">Probability</abbr></th>
                <th># Songs</th>
            </tr>
            </thead>
            <tbody>
            <? foreach($playlists as $row): ?>
                <tr class="vertical-align-middle">
                    <td>
                        <?=$this->viewHelper->button(array(
                            'type'      => 'small',
                            'class'     => 'btn-primary',
                            'href'      => $this->url->routeFromHere(array('action' => 'edit', 'id' => $row['id'])),
                            'icon'      => 'pencil',
                            'text'      => 'Edit',
                        )) ?>
                        <?=$this->viewHelper->button(array(
                            'type'      => 'small',
                            'href'      => $this->url->routeFromHere(array('action' => 'delete', 'id' => $row['id'])),
                            'class'     => 'btn-danger confirm-delete',
                            'icon'      => 'delete',
                            'text'      => 'Delete',
                        )) ?>
                    </td>
                    <td>
                        <big><?=$row['name'] ?></big>
                        <? if ($row['include_in_automation']): ?>
                            <br><span class="label label-success">Auto-Assigned</span>
                        <? endif; ?>
                    </td>
                    <td><?=$row['weight'] ?></td>
                    <td><?=$row['probability'] ?></td>
                    <td><?=count($row['media']) ?></td>
                </tr>
            <? endforeach; ?>
            </tbody>
        </table>
    </div>
</div>