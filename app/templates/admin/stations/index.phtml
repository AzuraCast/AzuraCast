<?php $this->layout('main', ['title' => _('Stations'), 'manual' => true]); ?>

<div class="card">
    <div class="card-header ch-alt">
        <h2><?=_('Manage Stations') ?></h2>

        <a class="btn bgm-blue btn-float" role="button" title="<?=sprintf(_('Add %s'), _('Station')) ?>" href="<?=$url->named('admin:stations:add') ?>"><i class="zmdi zmdi-plus"></i></a>
    </div>
    <div class="table-responsive">
        <table class="table table-striped">
            <colgroup>
                <col width="30%">
                <col width="70%">
            </colgroup>
            <thead>
            <tr>
                <th><?=_('Actions') ?></th>
                <th><?=_('Station') ?></th>
            </tr>
            </thead>
            <tbody>
            <?php foreach((array)$stations as $record): ?>
                <tr class="input">
                    <td class="center">
                        <a class="btn btn-sm btn-primary" href="<?=$url->named('stations:index:index', ['station' => $record['id']]) ?>" target="_blank"><?=_('Manage') ?></a>
                        <a class="btn btn-sm btn-default" href="<?=$url->named('admin:stations:edit', ['id' => $record['id']]) ?>"><?=_('Edit') ?></a>
                        <a class="btn btn-sm btn-default" href="<?=$url->named('admin:stations:clone', ['id' => $record['id']]) ?>"><?=_('Clone') ?></a>
                        <a class="btn btn-sm btn-danger" href="<?=$url->named('admin:stations:delete', ['id' => $record['id'], 'csrf' => $csrf]) ?>"><?=_('Delete') ?></a>
                    </td>
                    <td>
                        <big><?=$this->e($record['name']) ?></big>
                    </td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>