<?php $this->layout('main', ['title' => _('My Profile'), 'manual' => true]); ?>

<?php
$options = $form->getOptions();
?>

<div class="block-header">
    <h2><?=_('My Profile') ?></h2>
</div>
<div class="card" id="profile-main">
    <aside class="pm-overview c-overflow" role="region">

        <div class="pmo-pic">
            <div class="p-relative">
                <a href="http://www.gravatar.com/" target="_blank">
                    <img class="img-responsive" src="<?=$user->getAvatar(500) ?>" alt="">
                </a>
                <a href="http://www.gravatar.com/" target="_blank" class="pmop-edit">
                    <i class="zmdi zmdi-camera"></i>
                    <span class="hidden-xs"><?=_('Update Profile Picture') ?></span>
                </a>
            </div>
        </div>

        <div class="pmo-block">
            <a class="btn btn-block btn-primary" role="button" href="<?=$url->named('profile:edit') ?>"><?=_('Edit Profile') ?></a>
        </div>

        <div class="pmo-block pmo-contact hidden-xs">
            <h2><?=_('Roles') ?></h2>

            <ul>
            <?php foreach($user->getRoles() as $role): ?>
                <li><?=$this->e($role->getName()) ?></li>
            <?php endforeach; ?>
            </ul>
        </div>
    </aside>

    <div class="pm-body clearfix">
        <section class="pmb-block" role="region">
            <div class="pmbb-header">
                <h2><i class="zmdi zmdi-account m-r-10"></i> <?=_('Account Information') ?></h2>
            </div>
            <div class="pmbb-body p-l-30">
                <div class="pmbb-view">
                    <dl class="dl-horizontal">
                        <?=$this->fetch('partials/form_view_fields', ['form' => $form, 'fieldset' => $options['groups']['account_info']]) ?>
                    </dl>
                </div>
            </div>
        </section>

        <section class="pmb-block" role="region">
            <div class="pmbb-header">
                <h2><i class="zmdi zmdi-settings m-r-10"></i> <?=_('Customization') ?></h2>
            </div>
            <div class="pmbb-body p-l-30">
                <div class="pmbb-view">
                    <dl class="dl-horizontal">
                        <?=$this->fetch('partials/form_view_fields', ['form' => $form, 'fieldset' => $options['groups']['customization']]) ?>
                    </dl>
                </div>
            </div>
        </section>
    </div>
</div>
