<?php
$this->layout('main', [
    'title' => $title,
    'header' => $header
]);

/** @var \App\Assets $assets */
$assets
    ->load('chosen')
    ->load('zxcvbn')
    ->addInlineJs($this->fetch('system/form_page.js'));

if ($render_mode === 'edit') {
    echo $form->render();
} else {
    echo $form->renderView();
}
