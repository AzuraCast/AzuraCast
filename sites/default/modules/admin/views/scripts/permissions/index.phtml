<?
$title = 'Manage Permissions';
$manual = TRUE;
?>

<div class="row-fluid">
    <div class="span6">
        <div class="floating">
            <h2>Roles</h2>

            <div class="buttons btn-group">
                <?=$this->viewHelper->button(array(
                    'type'      => 'link',
                    'class'     => 'large success',
                    'href'      => $this->viewHelper->routeFromHere(array('action' => 'editrole')),
                    'icon'      => 'icon-plus-sign',
                    'text'      => 'Add Role',
                )) ?>
                <?=$this->viewHelper->button(array(
                    'type'      => 'link',
                    'class'     => 'large',
                    'href'      => $this->viewHelper->routeFromHere(array('action' => 'rolemembers')),
                    'icon'      => 'icon-user',
                    'text'      => 'Members',
                )) ?>
            </div>

            <table class="table table-striped">
                <colgroup>
                    <col width="40%" />
                    <col width="60%" />
                </colgroup>
                <tbody>
                <? foreach($roles as $role): ?>
                    <tr class="input">
                        <td class="center">
                            <?=$this->viewHelper->button(array(
                                'type'      => 'small',
                                'icon'      => 'icon-pencil',
                                'href'      => $this->viewHelper->routeFromHere(array('action' => 'editrole', 'id' => $role['id'])),
                                'text'      => 'Edit',
                            )) ?>
                        <? if( $role['id'] != 1 ): ?>
                            <?=$this->viewHelper->button(array(
                                'type'      => 'small',
                                'icon'      => 'icon-trash',
                                'class'     => 'danger confirm-delete',
                                'href'      => $this->viewHelper->routeFromHere(array('action' => 'deleterole', 'id' => $role['id'])),
                                'text'      => 'Delete',
                            )) ?>
                        <? else: ?>
                            <?=$this->viewHelper->button(array(
                                'type'      => 'small',
                                'icon'      => 'icon-trash',
                                'class'     => 'disabled',
                                'href'      => 'javascript:;',
                                'onclick'   => 'alert(\'This role is the super administrator and cannot be deleted.\'); return false;',
                                'text'      => 'Delete',
                            )) ?>
                        <? endif; ?>
                        </td>
                        <td align="left"><?=$role['name'] ?></td>
                    </tr>
                <? endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
    <div class="span6">
        <div class="floating">
            <h2>Actions</h2>

            <div class="buttons">
                <?=$this->viewHelper->button(array(
                    'type'      => 'link',
                    'class'     => 'large success',
                    'href'      => $this->viewHelper->route(array('module' => 'admin', 'controller' => 'permissions', 'action' => 'editaction')),
                    'icon'      => 'icon-plus-sign',
                    'text'      => 'Add Action',
                )) ?>
            </div>

            <table class="table table-striped">
                <colgroup>
                    <col width="40%" />
                    <col width="60%" />
                </colgroup>
                <tbody>
                <? foreach($actions as $action): ?>
                    <tr class="input">
                        <td class="center">
                            <?=$this->viewHelper->button(array(
                                'type'      => 'small',
                                'icon'      => 'icon-pencil',
                                'href'      => $this->viewHelper->routeFromHere(array('action' => 'editaction', 'id' => $action['id'])),
                                'text'      => 'Edit',
                            )) ?>
                            <?=$this->viewHelper->button(array(
                                'type'      => 'small',
                                'icon'      => 'icon-trash',
                                'class'     => 'danger confirm-delete',
                                'href'      => $this->viewHelper->routeFromHere(array('action' => 'deleteaction', 'id' => $action['id'])),
                                'text'      => 'Delete',
                            )) ?>
                        </td>
                        <td align="left"><?=$action['name'] ?></td>
                    </tr>
                <? endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>