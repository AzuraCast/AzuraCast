$(function () {

    $('textarea.css-editor,textarea.js-editor').each(function () {
        CodeMirror.fromTextArea($(this)[0], {
            lineNumbers: true,
            theme: 'default'
        });
    });
    
});
