<?php
$props = [
    'csrf' => $csrf,
    'listUrl' => $router->fromHere('stations:files:list'),
    'batchUrl' => $router->fromHere('stations:files:batch'),
    'uploadUrl' => $router->fromHere('stations:files:upload'),
    'listDirectoriesUrl' => $router->fromHere('stations:files:directories'),
    'mkdirUrl' => $router->fromHere('stations:files:mkdir'),
]
?>
// grid.find('.album-art').fancybox({});

var station_media_manager;

$(function () {
  station_media_manager = new Vue({
    el: '#media-manager',
    render: function (createElement) {
      return createElement(StationMedia.default, {
        props: <?=json_encode($props) ?>
      });
    }
  });
});