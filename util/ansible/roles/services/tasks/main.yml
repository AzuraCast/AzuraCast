---
- name: Enable and Restart Services
  service:
    name: "{{ item }}"
    enabled: true
    state: restarted
  ignore_errors: true
  with_items:
    - "supervisor"
    - "mysql"
    - "redis-server"
    - "php8.1-fpm"
    - "nginx"
    - "cron"
    - "sftpgo"
